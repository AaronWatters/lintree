# lintree
Interactive diagram representing cell division lineages over timesteps

`lineage_chart.js` exports `LineageChart` and `LineageCell` for rendering lineage diagrams with D3/SVG.

See `/home/runner/work/lintree/lintree/example.html` for a complete browser usage example.
